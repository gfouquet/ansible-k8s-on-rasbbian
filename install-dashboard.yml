---
- hosts: masters
  tasks:
    - import_role:
        name: k8s-dashboard

#    - name: todo install helm
#    - name: install tiller
#      command: 'helm init --tiller-image=jessestuart/tiller:v2.9.1'

